services:
  inspire_hand_teleop:
    image: inspire_hand_teleop:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: inspire_hand_teleop
    network_mode: host
    # privileged already exposes all host /dev nodes, so the RS485-USB adapter
    # (/dev/ttyUSB0 or /dev/ttyACM0) is reachable inside the container with no
    # extra mapping. If you ever drop privileged, add instead:
    #   devices:
    #     - /dev/ttyUSB0:/dev/ttyUSB0
    privileged: true
    environment:
      DISPLAY: ${DISPLAY}
      ROS_DOMAIN_ID: 0
      RMW_IMPLEMENTATION: rmw_cyclonedds_cpp
      # CycloneDDS config so g1_inspire_hand_bridge can reach the robot's Inspire
      # DDS topics. Edit docker/cyclonedds.xml to set your network interface.
      CYCLONEDDS_URI: file:///workspace/ros2_ws/cyclonedds.xml
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ../src:/workspace/ros2_ws/src
      - ./cyclonedds.xml:/workspace/ros2_ws/cyclonedds.xml
    stdin_open: true
    tty: true
